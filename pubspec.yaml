name: daftar_ktp
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  hive: ^2.0.4
  path_provider: ^2.0.1
  go_router: ^13.0.1
  bloc: ^7.0.0
  equatable: ^2.0.3
  flutter_bloc: ^7.0.0
  hive_flutter: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

assets:
  - assets/